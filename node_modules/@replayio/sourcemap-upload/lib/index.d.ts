export declare type MessageLevel = "normal" | "verbose";
export declare type LogCallback = (level: MessageLevel, message: string) => void;
export interface UploadOptions {
    filepaths: Array<string> | string;
    group: string;
    key?: string;
    dryRun?: boolean;
    extensions?: Array<string>;
    ignore?: Array<string>;
    root?: string;
    log?: LogCallback;
    server?: string;
}
export declare function uploadSourceMaps(opts: UploadOptions): Promise<void>;
